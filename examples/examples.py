'''
Remember to download the 'bettermaths.py' file in the 'src' file!
Import like this: import bettermaths as bm

bm.area2D_circle(radius) example:
print(bm.area2D_circle(8)) -> outputs 201.06192

bm.area2D_square(length) example:
print(bm.area2D_square(4)) -> outputs 16

bm.area2D_rectangle(width, length) example:
print(bm.area2D_rectangle(3, 4)) -> outputs 12

bm.area2D_triangle(base, perpendicular_height) example:
i am kinda lazy and don't want to make example for this rnow... see ya in the next commit!
'''